version: "3.8"

services:
  frontend:
    command: npm run dev
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "5173:5173"
    volumes:
      - .:/app:cached
      - /app/node_modules
